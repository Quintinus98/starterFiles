import express from "express";

const routes = express.Router();

// Route to get someUrl
// routes.get(
//   "/someUrl",
//   (req: express.Request, res: express.Response): void => {
    
//   }
// );

export default routes;
